ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Optional: VballNet ball detector (motion-based, no training needed)
onnxruntime>=1.16.0

# Optional: pose estimation
rtmlib>=0.0.13

# Optional: jersey number identification (--identify)
# easyocr usa torch, que ultralytics ya instala — no añade peso pesado nuevo.
easyocr>=1.7.0

# Optional: touch_machine.py post-procesador (detección de picos de aceleración)
scipy>=1.10.0
